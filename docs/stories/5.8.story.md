# Story 5.8 — Kiosk Token para TV Dashboard

| Campo | Valor |
|-------|-------|
| **ID** | 5.8 |
| **Epic** | 5 — Phase 1 Backend (Architecture v2.1) |
| **Sprint** | Fase 1 |
| **Dia** | 3-4 — Core APIs |
| **Prioridade** | P3 — VISIBILIDADE |
| **Complexidade** | S |
| **Status** | Ready |
| **Agente** | @dev |

## Descricao

Como administrador, preciso gerar tokens de kiosk para que TVs na fabrica exibam o dashboard de producao sem necessidade de login interativo, com seguranca por token UUID revogavel vinculado ao tenant.

## Criterios de Aceitacao

- [ ] **AC1:** Tabela `kiosk_tokens` criada: `{id, tenant_id, token (UUID), name, scope, is_active, created_at}`
- [ ] **AC2:** Endpoint `POST /api/admin/kiosk-tokens` cria token (somente ADMIN)
- [ ] **AC3:** Endpoint `GET /api/admin/kiosk-tokens` lista tokens do tenant (somente ADMIN)
- [ ] **AC4:** Endpoint `DELETE /api/admin/kiosk-tokens/:id` revoga token (somente ADMIN)
- [ ] **AC5:** Middleware de kiosk valida token da URL `?token=<uuid>`, extrai tenant_id, cria sessao read-only
- [ ] **AC6:** URL do kiosk: `liserie.lision.app/tv?token=<uuid>`
- [ ] **AC7:** Token sem expiracao automatica, mas revogavel via endpoint
- [ ] **AC8:** RLS em `kiosk_tokens` restrita ao tenant do admin
- [ ] **AC9:** Sessao kiosk permite apenas leitura de dados do dashboard (read-only)

## Escopo

**IN:**
- Tabela kiosk_tokens + migration
- CRUD de tokens (criar, listar, revogar)
- Middleware de validacao de token kiosk
- RLS para kiosk_tokens

**OUT:**
- Frontend do TV dashboard (ja existe no Lovable)
- Expiracao automatica de token
- Multiplos escopos de kiosk (apenas dashboard na Fase 1)
- QR code para facilitar setup da TV

## Dependencias

| Story | Motivo |
|-------|--------|
| 5.1 | Schema base aplicado |
| 5.2 | RLS patterns estabelecidos |
| 5.3 | Auth middleware para endpoints admin |
| 5.7 | KPIs endpoint que o kiosk vai consumir |

## Notas Tecnicas

- **Item 8 (v2.1):** TV Dashboard sem autenticacao e um risco CRITICO. Solucao: token UUID por tenant.
- **Tabela kiosk_tokens:**
  ```sql
  CREATE TABLE kiosk_tokens (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tenant_id UUID NOT NULL REFERENCES tenants(id),
    token UUID NOT NULL DEFAULT gen_random_uuid() UNIQUE,
    name TEXT NOT NULL,
    scope TEXT NOT NULL DEFAULT 'dashboard',
    is_active BOOLEAN NOT NULL DEFAULT true,
    created_at TIMESTAMPTZ NOT NULL DEFAULT now()
  );
  ```
- **Middleware:** Verificar `?token=` na URL, buscar em kiosk_tokens, verificar is_active, extrair tenant_id.
- **Sessao read-only:** O kiosk token nao cria sessao Supabase Auth — apenas fornece tenant_id para queries de dashboard.
- **Source:** `docs/architecture/ARCHITECTURE-REVIEW-v2.1.md` item 8

## Tasks

- [ ] **T1 (AC1):** Criar migration para tabela `kiosk_tokens`
- [ ] **T2 (AC8):** Criar RLS policies para `kiosk_tokens`
- [ ] **T3 (AC2):** Implementar `POST /api/admin/kiosk-tokens/route.ts`
- [ ] **T4 (AC3):** Implementar `GET /api/admin/kiosk-tokens/route.ts`
- [ ] **T5 (AC4):** Implementar `DELETE /api/admin/kiosk-tokens/[id]/route.ts`
- [ ] **T6 (AC5, AC9):** Criar middleware de validacao de kiosk token
- [ ] **T7 (AC6):** Configurar rota `/tv` que usa middleware kiosk
- [ ] **T8:** Testar fluxo: admin cria token -> TV acessa com token -> dashboard carrega -> admin revoga -> TV perde acesso

## File List

| Arquivo | Acao |
|---------|------|
| `prisma/schema.prisma` | Modificado — modelo KioskToken |
| `prisma/migrations/*/kiosk-tokens.sql` | Criado — tabela + RLS |
| `src/app/api/admin/kiosk-tokens/route.ts` | Criado — POST/GET tokens |
| `src/app/api/admin/kiosk-tokens/[id]/route.ts` | Criado — DELETE token |
| `src/lib/kiosk-middleware.ts` | Criado — Validacao de token kiosk |
| `src/app/(kiosk)/tv/page.tsx` | Criado — Rota TV dashboard |

## Dev Agent Record

### Debug Log References
_Atualizado durante implementacao_

### Completion Notes
_Atualizado durante implementacao_

## Change Log

| Data | Agente | Acao |
|------|--------|------|
| 2026-05-17 | @sm | Story criada baseada em ARCHITECTURE-REVIEW-v2.1.md |
| 2026-05-17 | @po | Validacao GO (9/10) — Status atualizado para Ready |
