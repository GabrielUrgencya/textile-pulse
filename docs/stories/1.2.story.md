# Story 1.2 — Controle de Acesso (M6)

| Campo | Valor |
|-------|-------|
| **ID** | 1.2 |
| **Módulo** | M6 — Controle de Acesso |
| **Sprint** | Fase 1 |
| **Dia** | 1 — Fundação |
| **Prioridade** | P0 — BLOQUEANTE |
| **Complexidade** | G |
| **Status** | Ready for Review |

## Descrição

Como administrador da fábrica, preciso controlar quem acessa o sistema com perfis hierárquicos (admin, gerente, operador) e login por PIN rápido para o chão de fábrica, garantindo segurança sem atrapalhar a operação.

## Critérios de Aceitação

- [ ] **AC1:** Login por email/senha para admin e gerentes
- [ ] **AC2:** Login por PIN (4-6 dígitos) para operadores no chão de fábrica
- [ ] **AC3:** 3 perfis implementados: `admin` (tudo), `gerente` (leitura + criação OP), `operador` (apenas bipagem + visualização)
- [ ] **AC4:** Middleware de rota protege todas as páginas — redirect para `/login` se não autenticado
- [ ] **AC5:** Sessão persiste por 8h (turno de fábrica), depois exige re-autenticação
- [ ] **AC6:** Tela de login responsiva funciona em tablet e desktop

## Escopo

**IN:**
- Autenticação email/senha (Supabase Auth)
- Autenticação por PIN (custom, tabela `users`)
- 3 perfis com permissões definidas
- Middleware de proteção de rotas
- Tela de login dual (email ou PIN)
- Gerenciamento de sessão com expiração

**OUT:**
- Recuperação de senha
- OAuth/SSO
- Cadastro self-service (admin cria usuários)
- Audit log de login (Fase 2)

## Dependências

| Story | Motivo |
|-------|--------|
| 1.1 | Schema de `users` e setup do Prisma/Supabase |

## Notas Técnicas

- PIN é armazenado como hash (bcrypt) na tabela `users`
- Middleware Next.js (`middleware.ts`) na raiz do projeto
- Perfil do usuário vem do campo `role` na tabela `users`
- Sessão via cookie httpOnly com JWT
- PIN mode: tela simplificada com teclado numérico grande (touch-friendly)
- **Design Guidelines:** `docs/ux/FRONTEND-DESIGN-GUIDELINES.md` — SEGUIR OBRIGATORIAMENTE para todos os componentes visuais
- **Componentes compartilhados:** Usar `LisionCard`, `PageHeader`, `StatusBadge`, `MetricBox`, `TopBar` de `src/components/`

## Tasks

- [x] **T1 (AC1, AC2, AC6):** Criar tela de login dual (email/senha + PIN) seguindo design guidelines
- [x] **T2 (AC4):** Expandir middleware para proteger páginas (não apenas /api/) — redirect para /login
- [x] **T3 (AC3):** Implementar helper de permissões por role (admin/gerente/operador)
- [x] **T4 (AC5):** Configurar sessão com expiração de 8h
- [x] **T5:** Validação final — tsc + build

## File List

| Arquivo | Ação |
|---------|------|
| `src/app/login/page.tsx` | Criar — tela de login dual (fora do route group `(app)` para não herdar AppShell/sidebar) |
| `src/middleware.ts` | Modificar — proteger páginas + redirect login |
| `src/lib/permissions.ts` | Criar — helper de permissões por role |
| `src/app/page.tsx` | Modificar — redirect para /login em vez de /dashboard |

## Dev Agent Record

### Agent Model Used
claude-opus-4-6

### Debug Log References
- Nenhum erro encontrado durante implementação

### Completion Notes
- T1: Tela de login dual criada em `src/app/(app)/login/page.tsx` — modo choose → email → pin, com AnimatePresence, PIN numpad 6 dígitos, auto-submit, branding LISION, ErrorBanner
- T2: Middleware expandido para proteger todas as páginas (não apenas /api/). Redirect para /login com query param `redirect`. Authenticated users em /login são redirecionados para /dashboard. Portal excluído (tem auth própria). Root `/` agora redireciona para `/login`
- T3: Helper `src/lib/permissions.ts` com 4 roles (ADMIN, GERENTE, COORDENADOR, OPERADOR), 13 permissões granulares, funções hasPermission, hasAllPermissions, hasAnyPermission, hasMinRole
- T4: Cookie maxAge configurado para 28800s (8h) com httpOnly, secure em produção, sameSite lax. Nota: JWT expiry do Supabase project deve ser configurado para 28800s no dashboard
- T5: `tsc --noEmit` e `next build` passaram sem erros

## Change Log

| Data | Agente | Ação |
|------|--------|------|
| 2026-03-09 | @po | Story criada |
| 2026-05-18 | @po | Validação GO (9/10) — Status Draft → Ready. Design guidelines referenciados. |
| 2026-05-18 | @dev | Implementação completa (T1-T5). tsc + build OK. Status → Ready for Review |
