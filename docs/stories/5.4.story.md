# Story 5.4 — API /api/scan — Bipagem por Lote

| Campo | Valor |
|-------|-------|
| **ID** | 5.4 |
| **Epic** | 5 — Phase 1 Backend (Architecture v2.1) |
| **Sprint** | Fase 1 |
| **Dia** | 3-4 — Core APIs |
| **Prioridade** | P1 — BLOQUEANTE |
| **Complexidade** | M |
| **Status** | Ready |
| **Agente** | @dev |

## Descricao

Como operador de chao de fabrica, preciso bipar lotes com scanner USB para registrar a passagem por cada estagio de producao, atualizando o status do lote e criando eventos de scan rastreraveis.

## Criterios de Aceitacao

- [ ] **AC1:** Endpoint `POST /api/scan` recebe `{ barcode, stage_id }` e cria `scan_event`
- [ ] **AC2:** Barcode validado contra formato `^OP-[0-9]{8}-[0-9]{3}-L[0-9]{3}$`
- [ ] **AC3:** Status do lote atualizado automaticamente para o estagio correspondente
- [ ] **AC4:** Scan duplicado (mesmo lote, mesmo estagio) rejeitado com erro descritivo
- [ ] **AC5:** `scan_event` registra: `lot_id`, `stage_id`, `user_id`, `scanned_at`, `device_info`
- [ ] **AC6:** Endpoint `GET /api/scan/history/:lotId` retorna historico de scans do lote
- [ ] **AC7:** Todas as queries usam Supabase client autenticado (NAO Prisma)
- [ ] **AC8:** RLS garante que operador so cria scans para lotes do seu tenant

## Escopo

**IN:**
- Endpoint POST /api/scan (criar scan event)
- Endpoint GET /api/scan/history/[lotId] (historico)
- Validacao de barcode
- Atualizacao de status do lote
- Protecao contra scan duplicado

**OUT:**
- Sync offline de scans (Item 15 v2.1 — Fase 2)
- Realtime notification de scan (Story 5.7)
- Scan de defeitos (parte do endpoint de defeitos)
- Interface de bipagem (frontend Lovable)

## Dependencias

| Story | Motivo |
|-------|--------|
| 5.1 | Schema com tabelas `scan_events`, `lots`, `stages` |
| 5.2 | RLS ativa para validacao de tenant |
| 5.3 | Auth middleware para proteger endpoints |

## Notas Tecnicas

- **Fluxo de bipagem:**
  1. Scanner USB envia barcode como input de teclado
  2. Frontend captura e envia POST /api/scan
  3. Backend valida barcode, verifica lote existe, verifica stage valido
  4. Cria scan_event, atualiza lot.current_stage_id e lot.status
  5. Retorna scan_event criado
- **Validacao de sequencia:** Na Fase 1, alertar mas nao bloquear scans fora de ordem (operacao da fabrica nao pode parar)
- **Quantidades computadas:** NAO atualizar campos de quantidade em production_orders (removidos no Item 5). Dashboard computa via query.
- **Source:** `docs/architecture/ARCHITECTURE-REVIEW-v2.1.md` item 5 (Opcao A)

## Tasks

- [ ] **T1 (AC1, AC5):** Implementar `POST /api/scan/route.ts` — criar scan_event via Supabase client
- [ ] **T2 (AC2):** Validar barcode contra regex no handler
- [ ] **T3 (AC3):** Atualizar `lots.current_stage_id` e `lots.status` apos scan valido
- [ ] **T4 (AC4):** Verificar duplicidade (mesmo lot_id + stage_id) e retornar 409 Conflict
- [ ] **T5 (AC6):** Implementar `GET /api/scan/history/[lotId]/route.ts`
- [ ] **T6 (AC7):** Garantir que todas as queries usam Supabase client, nao Prisma
- [ ] **T7 (AC8):** Testar que RLS bloqueia scan de lote de outro tenant
- [ ] **T8:** Testar fluxo completo: bipar lote -> verificar scan_event -> verificar status atualizado

## File List

| Arquivo | Acao |
|---------|------|
| `src/app/api/scan/route.ts` | Criado — POST scan event |
| `src/app/api/scan/history/[lotId]/route.ts` | Criado — GET historico |

## Dev Agent Record

### Debug Log References
_Atualizado durante implementacao_

### Completion Notes
_Atualizado durante implementacao_

## Change Log

| Data | Agente | Acao |
|------|--------|------|
| 2026-05-17 | @sm | Story criada baseada em ARCHITECTURE-REVIEW-v2.1.md |
| 2026-05-17 | @po | Validacao GO (9/10) — Status atualizado para Ready |
