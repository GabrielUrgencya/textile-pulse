# Story 5.1 — Schema Corrigido + Migrations

| Campo | Valor |
|-------|-------|
| **ID** | 5.1 |
| **Epic** | 5 — Phase 1 Backend (Architecture v2.1) |
| **Sprint** | Fase 1 |
| **Dia** | 1-2 — Fundacao |
| **Prioridade** | P0 — BLOQUEANTE |
| **Complexidade** | L |
| **Status** | Done |
| **Agente** | @data-engineer |

## Descricao

Como arquiteto do sistema, preciso que o schema do banco de dados seja corrigido conforme as decisoes da revisao arquitetural v2.1, removendo campos desnormalizados, adicionando constraints, e gerando migrations Supabase versionadas.

## Criterios de Aceitacao

- [x] **AC1:** Campos desnormalizados removidos de `production_orders`: `produced_quantity`, `stocked_quantity`, `defect_quantity`, `discarded_quantity`
- [x] **AC2:** Campos desnormalizados removidos de `factions`: `total_pieces_sent`, `total_pieces_returned`, `total_defects`, `defect_rate`
- [x] **AC3:** CHECK constraint `chk_payment_target` em `payment_periods` garantindo XOR entre `user_id` e `faction_id` — **N/A: tabela payment_periods nao existe no schema atual. Sera criada em fase futura.**
- [x] **AC4:** CHECK constraint `chk_barcode_format` em `lots` validando formato `^OP-[0-9]{8}-[0-9]{3}-L[0-9]{3}$`
- [x] **AC5:** Campo `deleted_at TIMESTAMPTZ` adicionado em `profiles` e `tenants` (LGPD soft delete)
- [x] **AC6:** Campo `hourly_rate` removido do schema (se existir) — **N/A: campo nao existia no schema.**
- [x] **AC7:** Campo `meta_coefficient` documentado — fonte da verdade em `reference_targets`, snapshot em `production_orders`
- [x] **AC8:** Migration Prisma gerada e aplicada com sucesso via `prisma migrate dev`
- [x] **AC9:** `prisma generate` gera tipos TypeScript sem erros
- [x] **AC10:** Seed atualizado para refletir novo schema (sem campos removidos) — **Seed ja estava correto, sem referencias aos campos removidos.**

## Escopo

**IN:**
- Correcao do schema.prisma conforme itens 5, 6, 11, 12, 17 da revisao v2.1
- Geracao de migration Prisma
- Atualizacao do seed.ts
- Validacao de tipos gerados

**OUT:**
- RLS policies (Story 5.2)
- Remocao do Prisma runtime (Story 5.3+)
- Triggers ou stored procedures
- Criacao de tabela `kiosk_tokens` (Story 5.8)

## Dependencias

| Story | Motivo |
|-------|--------|
| 1.1 | Schema original ja aplicado |

## Notas Tecnicas

- **Item 5 (v2.1):** OPCAO A escolhida — eliminar campos desnormalizados, computar via query sobre `scan_events`/`defect_records`/`faction_shipments`
- **Item 6 (v2.1):** CHECK constraint XOR em `payment_periods` — nao separar em duas tabelas
- **Item 12 (v2.1):** Barcode regex `^OP-[0-9]{8}-[0-9]{3}-L[0-9]{3}$` — `op_number` ja e unico por tenant
- **Item 17 (v2.1):** `deleted_at` para LGPD — soft delete em `profiles` e `tenants`
- **Prisma:** Usado APENAS para migrations e tipos. Runtime usara Supabase client (ADR-003 v2.1)
- **Source:** `docs/architecture/ARCHITECTURE-REVIEW-v2.1.md` items 5, 6, 11, 12, 17

## Tasks

- [x] **T1 (AC1, AC2):** Remover campos desnormalizados de `production_orders` e `factions` no schema.prisma
- [x] **T2 (AC3):** Adicionar raw SQL na migration para CHECK constraint `chk_payment_target` em `payment_periods` — **N/A: tabela nao existe**
- [x] **T3 (AC4):** Adicionar raw SQL na migration para CHECK constraint `chk_barcode_format` em `lots`
- [x] **T4 (AC5):** Adicionar `deleted_at DateTime?` em `profiles` e `tenants`
- [x] **T5 (AC6):** Verificar e remover `hourly_rate` de `profiles` se existir — **N/A: campo nao existia**
- [x] **T6 (AC7):** Documentar `meta_coefficient` com comentario no schema
- [x] **T7 (AC8):** Executar `prisma migrate dev --name v2.1-schema-corrections`
- [x] **T8 (AC9):** Executar `prisma generate` e verificar tipos
- [x] **T9 (AC10):** Atualizar `prisma/seed.ts` removendo referencias aos campos deletados — **Seed ja correto**
- [x] **T10:** Validar que `next build` passa sem erros de tipo

## File List

| Arquivo | Acao |
|---------|------|
| `prisma/schema.prisma` | Modificado — campos removidos, constraints adicionadas, soft delete |
| `prisma/seed.ts` | Verificado — sem alteracao necessaria |
| `prisma/migrations/20260517232142_v2_1_schema_corrections/migration.sql` | Criado — migration inicial v2.1 com CHECK constraints e COMMENT ON |
| `prisma.config.ts` | Existente — datasource config para Prisma 7 |
| `.env` | Corrigido — pooler host aws-0 → aws-1 |

## Dev Agent Record

### Debug Log References
- `.env` pooler host estava `aws-0-sa-east-1` mas API retornou `aws-1-sa-east-1` — corrigido
- Prisma 7 nao aceita `url`/`directUrl` no schema — usa `prisma.config.ts` (ja existia)
- `payment_periods` e `hourly_rate` nao existem no schema atual — marcados N/A
- Seed nao referenciava campos removidos — nenhuma alteracao necessaria

### Completion Notes
- Migration inicial criada e aplicada: `20260517232142_v2_1_schema_corrections`
- Schema corrigido: campos desnormalizados removidos (Item 5 v2.1 Opcao A)
- LGPD soft delete: `deleted_at` em tenants e profiles (Item 17 v2.1)
- CHECK constraint barcode format aplicado (Item 12 v2.1)
- COMMENT ON para documentacao inline (meta_coefficient, tenants, profiles)
- `prisma generate` OK, `next build` OK
- AC3 (payment_periods CHECK) e AC6 (hourly_rate) marcados N/A — artefatos nao existem no schema

## Change Log

| Data | Agente | Acao |
|------|--------|------|
| 2026-05-17 | @sm | Story criada baseada em ARCHITECTURE-REVIEW-v2.1.md |
| 2026-05-17 | @po | Validacao GO (9/10) — Status atualizado para Ready |
| 2026-05-17 | @data-engineer | Implementacao completa — todos os ACs atendidos |
| 2026-05-18 | @po | Story fechada — Epic 5 completo, deploy em producao confirmado |
