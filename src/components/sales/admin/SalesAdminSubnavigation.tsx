/**
 * Aposentado: a navegação do LISION Vendas agora vive na sidebar lateral
 * (SalesSidebar), com paridade visual ao AppSidebar do Lision principal.
 * Mantido como no-op para não quebrar imports existentes nas páginas.
 */
export function SalesAdminSubnavigation() {
  return null;
}
